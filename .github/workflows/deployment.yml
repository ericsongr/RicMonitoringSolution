# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Build, publish and deploy project to Simply
on:
  # push:
  #   branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build_and_deploy:
    name: Build package and deploy to SmarterASP.NET
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
    
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0
          
      - name: Install dependencies
        run: dotnet restore
        
      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Publish
        run: dotnet publish
        
      # - name: Test with .NET
      #   run: dotnet test

      - name: Deploy to Simply        
        uses: jahbenjah/SmarterASP.NET-web-deploy@1.0.0
        with:
          website-name: ricappssolutions-002-site5
          server-computer-name: https://win5077.site4now.net:8172
          server-username: ricappssolutions-002
          server-password: Pa$$w0rd
          source-path: \RicMonitoringAPI\bin\Release\net6.0
          
            